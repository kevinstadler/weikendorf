# weikendorf

## file administration vor ort

1. hotspot aufmachen, name `akdk` passwort `dkklasse`
2. nach einigen sekunden sollte die folgende adresse erreichbar sein: http://raspberrypi.local/files.php
3. login mit user `admin` passwort `admin@123`
4. beliebiger upload/l√schen von files, `config.txt` bitte unber√hrt lassen!

## setup

```
sudo apt-get install -y exfat-fuse exfat-utils python3-pip libsdl2-mixer-2.0-0 libsdl2-2.0-0
# libsdl2-ttf-2.0-0
pip install pygame
```

